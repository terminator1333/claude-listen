# Examples

Synthetic sample outputs that show what `/listen` produces end-to-end. Everything here is fabricated — no real people, meetings, or projects.

- [`q2-migration-planning/`](./q2-migration-planning/) — a 45-minute eng planning meeting, three speakers, project has a `CLAUDE.md` describing a database migration.

Use these as templates when writing feature requests ("I want the output to look more like X") or to get a feel for the format before running the skill on your own recording.
